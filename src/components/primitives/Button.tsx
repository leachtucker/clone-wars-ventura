import styled from 'styled-components';

export const Button = styled.button`
  display: contents;
  border: none;
  outline: none;
  background: none;
  cursor: default;
  color: inherit;
`;

export default Button;
